<%#
  Renders an info card with an icon, title, description, and optional content.
  
  Parameters:
  - icon: String - The name of the Tabler icon to use
  - title: String - The card title
  - description: String (optional) - The card description
  - class_name: String (optional) - Additional CSS classes
%>

<div class="<%= local_assigns[:class_name] %> bg-primary-800/50 backdrop-blur-sm rounded-xl border border-primary-700/50 p-4">
  <div class="flex items-center space-x-3 mb-3">
    <%= tabler_icon(icon, size: 20, class: 'text-secondary-400') %>
    <h3 class="text-primary-50 font-mono"><%= title %></h3>
  </div>

  <% if local_assigns[:description].present? %>
    <p class="text-primary-400 text-sm font-mono"><%= description %></p>
  <% end %>

  <%= yield if block_given? %>
</div> 